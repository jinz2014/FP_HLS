const char *CircuitName = "sample";
const char *filename = "sample_stats.txt";

// Total operation nodes
// sample:10 fir16:23 pos:15; HAL:11; rate:7
int NODE_NU = 5; 

// Initial number of port pairs
int PORT_NU = 2;

// adder and multiplier rate
int RATE[] = {1, 1, 1};

// adder and multiplier latency
int LATENCY[] = {11, 6, 26};
//int LATENCY[] = {6, 8, 17};

// Port resource usage (add, mul, div)
// If the operation is not used at the input port,
// then the element value is 0.
int PORTS[] = {1, 1, 0};

// Initial functional unit usage 
int FU[] = {1, 1, 1};

// Max number of input port pairs
int MAX_PORTS[] = {1, 1, 0};
